dao, svc를 묶어서 도메인영역이라고 한다. (백엔드에 속함)

스프링이 웹만을 개발하기 위한 프레임워크는 아니다.

일반 어플리케이션을 만드는 데도 스프링프레임워크를 사용하기도 한다.

dao에서 인터페이스를 만들고, 구현

svc에서도 인터페이스를 만들고 구현,

지금까지의svc는 연산로직은 없고 dao로 넘겨주는 역할만 한다.

컨트롤러는 홈컨트롤러, 프로덕트 컨트롤러 두가지가 있다.

지금 상황에서는 도메인영역에서 데이터가 만들어진다.

templetes에 view의 정보가 들어있고

view를 동적으로 만들기 위해서 thymeleaf가 사용된다.

product controller에 

요청 메소드 ,url로 요청 데이터를 보낸다.

요청 url, 쿼리 파라미터, 요청메세지 헤더, 요청메세지 바디 이렇게 4가지의 형태로 요청 데이터를 전달할 수 있다.

데이터를 서버에서 폼 객체를 통해서 받아온다.?

레스트 api는 ?

컨트롤러의 조회쪽에서는 url에 중괄호가 쳐저있다.

pathvariable이라는 어노테이션을 통해서 괄호를 읽을 수 있다.

쿼리스트링의 경우 param이라는 어노테이션을 통해서 읽는다.

모델이라는 객체에 키 밸류 형식으로 뷰단에서 접근할 수 있는 키와 데이터를 넣는다.

타임리프를 통해서 view에서 모델(객체)에 접근 할 수 있다.

객체에 접근한 다는 것은 곧 정보를 가져온 다는 것이다.

view가 없이 컨트롤러만 있더라도 테스트가 가능하다.

포스트맨을 통해서 실험이 가능했다.

컨트롤러와 포스트맨만 있으면 테스트가 가능하다.

spa: singlePageApplication

ssr의 경우 깜빡인다. 하지만 csr이면 spa이기에 화면 전환이 일어나지 않아서 깜빡이지 않는다.

json 포멧으로 전달하는 api를 rest api, http api 라고 함;.

rest api에서 바깥 중괄호가 {}로 떨어지는 map은 불편하다. set이나 list가 낫다.

컨트롤 알트 n = 리팩토링

반환타입이 불린타입일 경우 메소드명 is로 많이 짓는다.

template.queryforObject : 하나의 쿼리값을 매핑?

Integer.class = int의 타입을 명시하기위함.

'여러행인데 컬럼이 하나' Integer(Long)타입의 배열이나 리스트로 타입을 지정하면 된다.

타입을 받는 경우가 4가지가 있는데... 뭔지 모르겠네.

queryforObject의 경우는 없으면 예외를 발생시킨다.

productDaoImpl의 182행의 경우 무조건 0,또는 1을 반환하므로

예외를 지정해 줄 필요가 없었다.