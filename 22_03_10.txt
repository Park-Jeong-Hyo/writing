1. ThemeProvider 컴포넌트 역할
	컨텍스트, 최상위 컴포넌트에 위치해서 속성값으로 정의되면 해당 속성값이 자식 컴포넌트면 
	어디라도 접근할 수 있게 한다.

2. props?
	컴포넌트가 호출될 때 전달되는 객체로 속성과 컨텐츠정보(children)을 담고 있다.

3. state
	컴포넌트 내에서 정의되고 사용되는 데이터로 데이터가 변경되면 다시 컴포넌트가 렌더링 된다.